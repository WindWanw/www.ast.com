/**
 * 浏览器tab列表
 * @param {*} state 
 * @returns 
 */
const menuList = (state) => state.menuList;

/**
 * 侧边栏相关信息列表
 * @param {*} state 
 * @returns 
 */
const asideList = (state) => state.asideList;


export default {
    menuList,

    asideList
}